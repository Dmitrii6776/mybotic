[phases.setup]
nixPkgs = ["python310", "gcc", "ta-lib" ]

[phases.build]
env = {
  TA_LIBRARY_PATH = "/nix/store/*ta-lib*/lib"
  TA_INCLUDE_PATH = "/nix/store/*ta-lib*/include"
}

[start]
cmd = "python3.10 -m freqtrade trade --config user_data/config.json --strategy hype_strategy"